import { LearningWayIconPipe } from './learning-way-icon.pipe';

describe('LearningWayIconPipe', () => {
  it('create an instance', () => {
    const pipe = new LearningWayIconPipe();
    expect(pipe).toBeTruthy();
  });
});
